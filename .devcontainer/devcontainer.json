{
  "name": "SGE Docker Codespace",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "postStartCommand": "docker-compose up -d && docker exec php composer install",
  "forwardPorts": [80, 3306, 8080],
  "features": {
    "docker-in-docker": "latest"
  }
}
