{
    "name": "SGE",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
    "postCreateCommand": "if [ ! -f vendor/autoload.php ]; then composer install --no-interaction; fi",
    "forwardPorts": [80, 8080, 3306]
}
